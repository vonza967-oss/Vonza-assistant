// Example only. Tool keys are metadata declarations, not executable tools.
// Use keys from src/services/tools/toolRegistry.js.
export const exampleToolKeys = Object.freeze([
  "common.lead_capture",
  "common.contact_route",
  "common.booking_link",
  "common.human_handoff",
]);

export default exampleToolKeys;
